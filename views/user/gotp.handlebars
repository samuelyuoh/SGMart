<div class="container d-flex flex-column">
  <div class="row align-items-center justify-content-center
      min-vh-75 g-0"  style="padding-top: 7.5rem; top: 0;">
    <div class="col-12 col-md-8 col-lg-4 border-top border-3 border-primary" style="color:grey;">
      <div class="card shadow-sm">
        <div class="card-body">
          <div class="mb-4">
            <h1 style="text-align:center ; padding-bottom: 30px">Verify OTP</h1>
            <h5 class="mb-2">Enter your Google Authenticator OTP to log in.</h5>
          </div>
          <form method="POST">
            <div class="mb-3">
              <label for="otp" class="form-label">OTP</label>
              <input type="text" id="gotp" class="form-control" name="gotp" placeholder="Enter Your OTP"
                required="">
            </div>
            <div class="mb-3 d-grid">
              <button type="submit" class="btn btn-primary" style="background-color: var(--green)">
                Verify OTP
              </button>
            </div>
          </form>
        </div>
      </div>
    </div>
  </div>
</div>

<style>
  p {
    margin-bottom: 0 !important;
  }

  .border-primary {
    border-color: var(--green) !important;
}
h5 {
  font-size: 15px;
}
</style>