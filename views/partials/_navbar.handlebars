<nav>
    <div class="header-1">

        <a class="logo" href=""><i class="fas fa-shopping-basket"></i>SGMart</a>

        <form action="" class="search-box-container">
            <input type="search" type="search" placeholder="Search" aria-label="Search" name="q" id="search-box">
            <label for="search-box" class="fas fa-search" type="submit"></label>
        </form>

    </div>

    <div class="header-2">

        <div id="menu-bar" class="fas fa-bars"></div>

        <nav class="navbar">
            <a href="/">Home</a>

            <a href="/product/products">Products</a>
            
            <div class="nav-item dropdown">
              <a class="nav-link dropdown-toggle" href="#" type="button" id="dropdownMenuLink" data-bs-toggle="dropdown" aria-expanded="false">Brands</a>
                <div class="dropdown-menu">
                  {{!-- {% for brand in brands %} --}}
                  {{#each brands}}
                  <a class="dropdown-item" href="" >{{brand_name}}</a>
                  {{/each}}
                  {{!-- {% endfor %} --}}
                </div>
            </div>

            <div class="nav-item dropdown">
              <a class="nav-link dropdown-toggle" href="#" type="button" id="dropdownMenuLink" data-bs-toggle="dropdown" aria-expanded="false">Categories</a>
                <div class="dropdown-menu">
                  {{!-- {% for category in categories %} --}}
                  <a class="dropdown-item" href="" >category name</a>
                  {{!-- {% endfor %} --}}
                </div>
            </div>

            <a href="">Contact</a>
            <a href="/blog/viewBlogs">Blogs</a>


            {{!-- <a href="/usercoupongenerate">Coupon(User)</a> --}}
            
        </nav>
        
        <div class="icons">
          <a class="fas fa-user-circle nav-link" type="button" data-bs-toggle="dropdown" id="dropdownMenuLink" aria-expanded="false" href="#" style="font-size: 25px !important; width: 25px !important; display: inline-block !important;"></a>
          {{#if user}}
            <div class="dropdown-menu">
                  <a class="dropdown-item" href="/user/profile/{{user.id}}">Account</a>
                  {{#if (isStaff user.userType)}}
                  <a class="dropdown-item" href="/Admin">Admin</a>
                  {{/if}}
                  <a class="dropdown-item" href="/user/logout"  style="color: red;">Logout</a>
            </div>
          {{else}}
            <div class="dropdown-menu">
                  <a class="dropdown-item" href="/user/login">Login</a>
                  <a class="dropdown-item" href="/user/register">Register</a>
            </div>
          {{/if}}

          <a class="fas fa-shopping-cart" href="/cart/cart" tabindex="-1" style="font-size: 25px !important;"></a>
        </div>

    </div>
</nav>